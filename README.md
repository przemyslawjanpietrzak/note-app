# Note app

## Gettings started

### Front
```bash
cd front
yarn install --pure-lockfile
yarn build
yarn deploy
cd ..
```

### Backend
```bash
cd backend
yarn install --pure-lockfile
node server.js
```

App will be hosted on port 4000